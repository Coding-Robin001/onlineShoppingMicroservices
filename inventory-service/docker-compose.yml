version: '3.8'

services:
  mysql:
    image: mysql:8
    container_name: mysql-inventory-service
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: Adewole171200
      MYSQL_DATABASE: inventory_service_schema
    ports:
      - "3308:3306"
    networks:
      - microservice-net   # <-- list format

networks:
  microservice-net:
    driver: bridge         # <-- proper indentation outside services
